extends layout

block content
  .container
    h2 Se parte de resuelvemitarea.com
    p Para poder publicar tu tarea y obtener quien la resuelva o para poder trabajar resolviendo tareas
      | debes primero registrate :)
    p Recuerda que puedes permanecer totalmente anonimo y sólo necesitas un nombre de usuario y una contraseña
    form#registro(method="POST")
      .form-group
        label Nombre(Nickname, no tu nombre real):
        input.form-control(type="text", name="nombre", placeholder="No tu nombre real")
      .form-group
        label Contraseña:
        input.form-control(type="password", name="password")
      input.btn.btn-primary(type="submit", value="Registrate")